<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="login.css">
    <link rel="stylesheet" href="dashboard.css">
    {{>fonts_icons}}
    <title>Dashboard</title>
</head>

<body>
    {{>header}}
    <h1>Dashboard</h1>

    <div class="addstory">
        <a href="/stories/add">Add Your Story <i class="fa-solid fa-file-circle-plus"></i></a>
    </div>
    {{#if stories}}

    <div class="storyContainer">
        {{#each stories}}
        <div class="card">
            <div class="info">
                <a href='/stories/{{_id}}' class="storyTitle">
                    <h2> {{title}} </h2>
                </a>
                <div id="share">
                    <p class="status"> {{status}} </p>
                    <ion-icon name="logo-twitter"></ion-icon>
                </div>
            </div>
            <div class="content">
                <p class="contentbody">{{body}}</p>
            </div>
            <div class="edits">
                <a href="/stories/read/{{_id}}" class="child">Read More</a>
                <a href="/stories/{{_id}}" class="child">EDIT</a>
                <form action="/stories/{{_id}}" method="POST">
                    <input type="hidden" name="_method" value="DELETE">
                    <button class='child' type="submit">DELETE</button>
                </form>
            </div>
        </div>
        {{/each}}
    </div>
    {{else}}
    <div class="storyContainer">

        <p>You have not created any stories</p>
    </div>

    {{/if }}
    {{>footer}}
    <script src="script.js"></script>
</body>

</html>